import { aZ as UndoElementInfo, a_ as UndoDetectorConfig, av as FormState } from './types-CXCbCmRP.js';
export { an as AIDiscoveredElement, a$ as AIElementRegistrationOptions, b0 as AIErrorContext, b1 as AIFindResponse, b2 as AccessibilityIssue, b3 as AccessibilityReport, b4 as AccessibilitySeverity, b5 as ActionBrowserEvent, b6 as ActionChanges, X as ActionDiffResult, b7 as ActionErrorCode, b8 as ActionErrorDiff, b9 as ActionExecutor, ba as ActionFailureDetails, bb as ActionHandler, A as ActionRequest, d as ActionResponse, V as ActionWithDiffRequest, bc as AggregatedErrors, i as AnyCapturedEvent, bd as AriaCheckedState, K as AssertionRequest, L as AssertionResult, ao as AssertionType, be as AutocompleteAction, k as BatchActionRequest, l as BatchActionResponse, bf as BatchActionStep, bg as BatchActionStepResult, M as BatchAssertionRequest, O as BatchAssertionResult, bh as BranchCondition, e as BridgeEvent, bi as BridgeEventListener, B as BridgeEventType, c as BridgeSnapshot, ap as BrowserCaptureConfig, bj as BrowserCapturedEvent, n as BrowserEventType, bk as BufferEntry, bl as BufferedChange, bm as BufferedRouteChange, m as CapturedError, aS as CapturedToast, _ as CategorizedDiff, a0 as ChangeBufferDrainResult, bn as ChangeCategory, Y as ChangePredicate, bo as ChangeTimeline, bp as ClassifiedEvent, bq as CompactElement, br as CompactModal, bs as CompactToast, bt as ComparisonIssue, bu as ComponentAction, v as ComponentActionRequest, g as ComponentActionResponse, bv as ComponentComparison, ac as ComponentInfo, bw as ComponentMatch, bx as ComponentStateResponse, by as ComputedProperty, bz as ConsoleCapturedEvent, bA as ContentChanges, bB as ContentComparison, bC as ContentMetadata, bD as ContentRole, bE as ContentType, u as ControlActionRequest, C as ControlActionResponse, bF as ControlBatchResponse, bG as ControlBatchStep, bH as ControlBatchStepResult, o as ControlSnapshot, ad as CrossAppComparisonReport, bI as CrossAppDiff, bJ as CrossAppMetricChange, bK as CrossAppStatusChange, bL as CustomAction, bM as DEFAULT_CAPTURE_CONFIG, bN as DEFAULT_NOISE_PATTERNS, bO as DEFAULT_REMOUNT_CACHE_WINDOW_MS, bP as DataType, bQ as DataValueComparison, ax as DeclaredUndoState, bR as DensityComparison, D as DetectedErrorOverlay, aF as DeveloperPageContext, bS as DiffSummaryOptions, am as DiscoveredElement, bT as DiscoveryRequest, bU as DiscoveryResponse, bV as DomMetricsCapturedEvent, bW as DragAction, bX as DragDropOrigin, bY as DragEffect, aW as DragSourceInfo, aX as DropZoneInfo, bZ as ElementAccessibility, b_ as ElementAssertionFailure, b$ as ElementAssertionResult, c0 as ElementAssertionSpec, c1 as ElementBbox, c2 as ElementChange, ag as ElementDesignData, c3 as ElementEventLog, c4 as ElementEventLogConfig, c5 as ElementFieldChange, c6 as ElementFingerprintData, r as ElementHistoryOptions, c7 as ElementIdentifier, s as ElementLogEntry, c8 as ElementLogLevel, c9 as ElementModification, aU as ElementRelationship, at as ElementState, ca as ElementType, cb as ErrorImpact, cc as ErrorImpactAssessor, cd as ErrorImpactConfig, E as ErrorSeverity, ce as ExtendedComputedStyles, cf as ExtendedWorkflowStep, cg as ExtractConfig, ch as ExtractedDataValue, ci as FallbackScreenshot, cj as FillAction, ck as FillFieldResult, F as FillFormRequest, j as FillResult, w as FindRequest, x as FindResponse, cl as FormAnalysis, cm as FormFieldAnalysis, cn as FormFieldState, co as FormatDescriptor, cp as FormatMismatch, ak as FormsResponse, cq as FreezeCapturedEvent, cr as GridDiff, cs as GridStructure, ct as GroupedElements, cu as HeadingChange, cv as HeadingLevelComparison, cw as HeadingMatch, cx as HierarchyDiff, cy as HmrCapturedEvent, cz as InlineRelationship, a6 as Intent, a4 as IntentExecutionResult, cA as IntentParam, a5 as IntentSearchResponse, cB as InteractionParity, ah as InteractionStateName, cC as KeyboardAction, aI as KeyboardShortcut, cD as LayoutComparison, cE as ListChangeAnalysis, cF as ListItemField, cG as ListSchema, cH as LoadingState, cI as LogConfig, cJ as LongTaskCapturedEvent, cK as LoopConfig, cL as MatchedElementPair, cM as MediaMetadata, cN as MediaType, cO as MemoryCapturedEvent, cP as MetricChange, cQ as MetricMatch, aK as ModalDetectorConfig, aN as ModalInfo, aL as ModalStack, cR as ModalState, cS as MouseAction, I as NLActionRequest, J as NLActionResponse, cT as NavigationCapturedEvent, aG as NavigationCompleteData, aA as NavigationEventData, cU as NavigationMap, cV as NavigationPair, N as NavigationResult, az as NavigationTrackerOptions, cW as NavigationTrigger, cX as NetworkCapturedEvent, cY as NormalizedRect, aq as OnBrowserEventCallback, cZ as OnCaptureCallback, c_ as PageContext, a9 as PageDataMap, aB as PageInfo, af as PageNavigateRequest, aC as PageNavigationEntry, ae as PageNavigationResponse, c$ as PageRegion, aa as PageRegionMap, d0 as ParsedAction, d1 as PartialMatch, P as PathResult, d2 as PseudoElementStyles, d3 as ReactErrorCapturedEvent, d4 as ReactStateInfo, d5 as RecoveryAction, a7 as RecoveryAttemptRequest, a8 as RecoveryAttemptResult, d6 as RecoverySuggestion, d7 as RegionType, d8 as RegisteredComponent, as as RegisteredElement, d9 as RelationshipOrigin, aT as RelationshipType, da as RepeatPatternData, db as ResourceErrorCapturedEvent, aj as ResponsiveSnapshot, aE as RouteInfo, dc as SEVERITY_RANK, dd as ScreenAnalysis, de as ScrollAction, df as ScrollIntoViewAction, dg as ScrollLogicalPosition, G as SearchCriteria, H as SearchResponse, au as SearchResult, dh as SelectAction, R as SemanticDiff, a2 as SemanticSearchCriteria, a3 as SemanticSearchResponse, di as SemanticSearchResult, Q as SemanticSnapshot, dj as SendKeysAction, dk as ServerBatchOperation, dl as ServerBatchOperationResult, dm as ServerBatchOptions, dn as ServerBatchResponse, dp as ShortcutSource, aH as ShortcutTrackerOptions, a1 as SnapshotBookmark, aY as SnapshotDragDropContext, dq as SnapshotEnricher, dr as SnapshotEnrichers, ds as SnapshotErrorSummary, aM as SnapshotModalContext, aD as SnapshotPageContext, dt as SnapshotRegistrationMetadata, aV as SnapshotRelationshipContext, aJ as SnapshotShortcutContext, aR as SnapshotToastContext, ay as SnapshotUndoContext, du as SnapshotViewportContext, dv as StandardAction, dw as StateGetter, S as StateSnapshot, ai as StateStyles, dx as StatusChange, dy as StatusMatch, $ as StructuredChangeAnalysis, ab as StructuredDataExtraction, dz as StyleDiff, dA as TableChangeAnalysis, dB as TableColumn, dC as TableComparison, dD as TableSchema, dE as TextChange, dF as TimelineEvent, aO as ToastCaptureConfig, aP as ToastEventData, dG as ToastLevel, aQ as ToastSnapshot, T as TransitionResult, dH as TypeAction, t as UIBridgeConfig, dI as UIBridgeFeatures, ar as UIBridgeRegistry, dJ as UIConsequences, U as UIState, p as UIStateGroup, dK as UIStateSnapshot, q as UITransition, dL as UndoDetectionSource, dM as UndoEntry, al as UndoRedoState, aw as UndoTrackerConfig, dN as UseDragSourceOptions, dO as UseDropZoneOptions, dP as UseUIRelationshipOptions, dQ as WCAGLevel, dR as WSChangeEventMessage, W as WSClientConfig, f as WSClientMessage, dS as WSClientMessageType, a as WSConnectionState, dT as WSDiscoverMessage, dU as WSErrorMessage, dV as WSEventMessage, dW as WSExecuteActionMessage, dX as WSExecuteComponentActionMessage, dY as WSExecuteWorkflowMessage, dZ as WSFindMessage, d_ as WSGetElementHistoryMessage, d$ as WSGetElementMessage, e0 as WSGetSnapshotMessage, e1 as WSMessageBase, e2 as WSPingMessage, e3 as WSPongMessage, e4 as WSRecordingAutoSaveMessage, e5 as WSRecordingRecoverMessage, e6 as WSRecordingStartMessage, e7 as WSRecordingStatusMessage, e8 as WSRecordingStopMessage, e9 as WSResponseMessage, ea as WSServerMessage, eb as WSServerMessageType, ec as WSSubscribeMessage, ed as WSSubscribedMessage, b as WSSubscriptionOptions, ee as WSUnsubscribeMessage, ef as WSUnsubscribedMessage, eg as WSWelcomeMessage, eh as WSWorkflowProgressMessage, Z as WaitForChangeOptions, ei as WaitForNavigationParams, ej as WaitForNavigationResult, ek as WaitOptions, el as WaitResult, em as WebVitalCapturedEvent, en as Workflow, eo as WorkflowEngine, y as WorkflowRunRequest, z as WorkflowRunResponse, ep as WorkflowRunStatus, eq as WorkflowStep, h as WorkflowStepResult, er as WorkflowStepType, es as WsDisconnectionCapturedEvent, et as classifyEvent, eu as classifyEvents, ev as computeAllFingerprints, ew as computeElementFingerprint, ex as computeFingerprintsWithMapping, ey as filterBySeverity, ez as findNearestRegisteredElement, eA as getActiveOverlays, eB as getGlobalRegistry, eC as installFrameworkOverlayCapture, eD as serializeRegisteredElement } from './types-CXCbCmRP.js';
export { ElementMatch, ElementQuery, FindableElement, QueryResult, Recency, Recency as RecencyType, StableElementRef, UIQuery, classList, classString, createStableRef, findElements, isSatisfiedBy, mightRequireFetch, parseRecency, requiresFetch, resolveStableRef } from './core/index.js';
export { C as ChangeObserver, a as ChangeObserverConfig } from './change-observer-hiF2OiFX.js';
export { U as UIBridgeWSClient, c as createWSClient } from './websocket-client-Bi8xeRcP.js';
export { AutoRegisterOptions, AutoRegisterProvider, AutoRegisterProviderProps, CaptureHostFrame, CaptureHostFrameProps, CommandRelayListener, CommandRelayListenerProps, ComponentActionDef, ComputedPropertyDef, ContentDiscoveryOptions, DEFAULT_CAPTURE_HOST_IDS, IREffect, IRElementCriteria, IRMetadata, IRProvenance, IdStrategy, MediaDiscoveryOptions, ShortcutDef, State, StateProps, TransitionTo, TransitionToProps, UIBridgeComponentScope, UIBridgeComponentScopeProps, UIBridgeContextValue, UIBridgeProvider, UIBridgeProviderProps, UI_BRIDGE_CONTENT_ATTR, UI_BRIDGE_ID_ATTR, UI_BRIDGE_PERSIST_ATTR, UI_BRIDGE_ROLE_ATTR, UI_BRIDGE_TEST_ID_ATTR, UseBuildIdWatcherOptions, UseCommandRelayOptions, UseUIBridgeEchoOptions, UseUIBridgeReturn, UseUIComponentOptions, UseUIComponentReturn, UseUIElementOptions, UseUIElementReturn, UseUINavigationReturn, UseUIStateGroupOptions, UseUIStateGroupReturn, UseUIStateOptions, UseUIStateReturn, UseUITransitionOptions, UseUITransitionReturn, pollForTaggedElement, trackElementBbox, useActiveStates, useAutoRegister, useAvailableTransitions, useBuildIdWatcher, useCanNavigateTo, useCommandRelay, useDragSource, useDropZone, useKeyboardShortcuts, useNavigationPath, useOwningComponent, usePageContext, useRouteAwareness, useStateSnapshot, useTransitions, useUIAnnotation, useUIBridge, useUIBridgeContext, useUIBridgeEcho, useUIBridgeOptional, useUIBridgeRequired, useUIComponent, useUIComponentAction, useUIElement, useUIElementRef, useUINavigation, useUIRelationship, useUIRelationships, useUIState, useUIStateGroup, useUITransition, useUndoRedo } from './react/index.js';
export { DefaultActionExecutor, DefaultWorkflowEngine, MAX_BATCH_SIZE, batch, controlBatch, createActionExecutor, createWorkflowEngine, extractReactState, fillFormFields } from './control/index.js';
export { CaptureOptions, CapturedElement, ChangeEntry, DOMChange, DOMChangeObserver, DOMSnapshot, ErrorEntry, InMemoryRenderLogStorage, InteractionEntry, NavigationEntry, RenderLogEntry, RenderLogEntryType, RenderLogManager, RenderLogOptions, RenderLogStorage, SnapshotEntry, captureDOMSnapshot, captureInteractiveElements, createRenderLogManager } from './render-log/index.js';
export { ClickHighlightOptions, ElementInfo, HIGHLIGHT_COLORS, InfoPanel, InfoPanelProps, Inspector, InspectorOverlay, InspectorOverlayProps, InspectorProps, InspectorState, UseInspectorOptions, getEventStack, showClickHighlight, showElementHighlight, useInspector } from './debug/index.js';
export { B as BrowserEventCapture, M as MemoryTrendAnalyzer, a as MemoryTrendResult } from './browser-capture-CWqqR7OI.js';
export { A as ActionTimelineEntry, B as BaselineComparison, b as BrowserEventCaptureLike, c as BrowserEventTimelineEntry, C as ClassifiedBrowserEvent, d as CorrelatedError, e as ErrorBaseline, f as ErrorDiff, g as ErrorSession, h as ErrorSessionManager, E as ErrorSessionSummary, a as ErrorSnapshot, i as ErrorSnapshotBuffer, j as ErrorSnapshotConfig, k as ErrorSnapshotPageState, F as FingerprintedEvent, H as HealthReport, l as HealthScoreConfig, m as HealthStatus, N as NetworkChain, n as NetworkChainConfig, o as NetworkChainTracker, p as NetworkRequest, q as NetworkResponse, r as TimelineBuffer, T as TimelineEntry, s as TimelineEntryType, t as TimelineQueryOptions, u as computeFingerprint, v as computeHealthReport, w as computeHealthScore, x as computeHealthStatus, y as deduplicateEvents, z as extractMessage, D as extractSourceLocation } from './error-snapshot-wfvZ-MCP.js';
export { B as BrowserEventStream, a as BrowserEventStreamMessage, b as StreamConfig, S as StreamSubscription } from './ws-streaming-DKZ5iHe4.js';
export { A as ActionHistoryEntry, M as MetricsCollector, a as MetricsCollectorOptions, P as PerformanceMetrics, c as createMetricsCollector, f as formatDuration, b as formatPercentage } from './metrics-CQrZWGSB.js';
export { ActionParityConfig, AliasGeneratorConfig, AliasGeneratorInput, BackgroundObserver, BackgroundObserverConfig, BackgroundObserverDeps, BookmarkStore, ChangeTracker, ChangeTrackerConfig, ChangeTrackerDeps, ComparisonReportConfig, ComponentComparisonConfig, ContentComparisonConfig, CrossAppDiffConfig, DEFAULT_ACTION_PARITY_CONFIG, DEFAULT_ALIAS_CONFIG, DEFAULT_COMPARISON_REPORT_CONFIG, DEFAULT_COMPONENT_COMPARISON_CONFIG, DEFAULT_CONTENT_COMPARISON_CONFIG, DEFAULT_CROSS_APP_DIFF_CONFIG, DEFAULT_DATA_EXTRACTION_CONFIG, DEFAULT_DIFF_CONFIG, DEFAULT_EXECUTOR_CONFIG, DEFAULT_FORMAT_ANALYSIS_CONFIG, DEFAULT_FUZZY_CONFIG, DEFAULT_LAYOUT_COMPARISON_CONFIG, DEFAULT_NAVIGATION_MAP_CONFIG, DEFAULT_REGION_SEGMENTATION_CONFIG, DEFAULT_SNAPSHOT_CONFIG, DEFAULT_TABLE_EXTRACTION_CONFIG, DEFAULT_VIEWPORTS, DataExtractionConfig, DesignRegistryLike, DetectedValidationError, ElementCaptureOptions, ElementSnapshot, ErrorCode, ErrorCodes, FormDiscoveryElement, FormatAnalysisConfig, FuzzyMatchConfig, FuzzyMatchResult, LayoutComparisonConfig, MediaAccessibilityAudit, MediaAnalysisResult, MediaAnalyzeBatchRequest, MediaAnalyzePageRequest, MediaAnalyzeRequest, MediaComparisonResult, MediaPerformanceAudit, MediaSnapshotData, NLActionExecutor, NLActionExecutorConfig, NLAssertionInput, NLAssertionOutput, NavigationMapConfig, NetworkProbeOptions, NetworkProbeRequest, NetworkProbeResult, PollWaitForElementOptions, PollWaitForElementOutcome, RegionSegmentationConfig, SemanticDiffConfig, SemanticDiffManager, SemanticSnapshotConfig, SemanticSnapshotManager, SerializedElementState, SnapshotBookmarkEntry, SummaryConfig, TableExtractionConfig, TimelineCapturePayload, VisualRegressionOptions, VisualRegressionResult, WAIT_FOR_ELEMENT_STATES, WaitForElementFoundResult, WaitForElementOptions, WaitForElementRequest, WaitForElementResult, WaitForElementState, WaitForElementTimeoutResult, WaitForOptions, WaitForPredicate, WaitForResult, __resetGlobalBookmarkStoreForTest, analyzeActionParity, analyzeFormat, analyzeMediaBatch, analyzeMediaElement, analyzeMediaPage, analyzePageFormats, analyzeStructuredChanges, areSynonyms, buildAccessibilityAudit, buildNavigationMap, buildPerformanceAudit, captureElementScreenshot, captureMediaSnapshot, captureResponsiveSnapshots, captureStateVariations, checkContrastCompliance, classifyDataType, classifyRegionType, classifyStatusDirection, compareComponents, compareContent, compareFormats, compareLayouts, compareMediaSnapshots, compareVisualRegression, computeContrastRatio, computeCrossAppDiff, computeDiff, computeProminence, createBrokenImagesFindRequest, createChangeTracker, createDiffManager, createErrorContext, createMediaFindRequest, createMissingAltFindRequest, createNLActionExecutor, createOversizedImagesFindRequest, createSimpleError, createSnapshotManager, describeAction, describeDiff, detectFormatPattern, detectGridStructure, detectList, detectTable, discoverForms, evaluateElementPredicate, extractModifiers, extractPageData, extractStructuredData, findAllMatches, findBestMatch, formatErrorContext, fuzzyContains, fuzzyMatch, generateAliases, generateComparisonReport, generateDescription, generateDiffSummary, generateElementDescription, generateNgrams, generatePageSummary, generatePurpose, generateSnapshotSummary, generateSuggestedActions, getBestRecoverySuggestion, getCSSCustomProperties, getElementDesignData, getExtendedComputedStyles, getGlobalBookmarkStore, getSynonyms, hasSignificantChanges, inferPageType, isNavigationElement, isRecoverableError, jaroSimilarity, jaroWinklerSimilarity, levenshteinDistance, levenshteinSimilarity, matchElements, networkProbe, ngramSimilarity, normalizeString, normalizeValue, parseNLAssertion, parseNLInstruction, parseNLInstructions, parseNumericValue, pollWaitForElement, scanValidationErrors, segmentPageRegions, serializeSnapshot, snapshotFromRegisteredElement, splitCompoundInstruction, tokenSimilarity, tokenize, validateParsedAction, validateWaitForElementRequest, waitFor, waitForElement, wordSimilarity } from './ai/index.js';
export { D as DEFAULT_SEARCH_CONFIG, d as DecomposedTarget, e as FindCandidate, F as FindContext, f as FindOptions, a as FindResult, g as FindResultAmbiguous, h as FindResultMatch, i as FindResultNotFound, c as FormDiff, j as FormFieldDiff, b as FormSnapshot, S as SearchEngine, k as SearchEngineConfig, l as SpatialRelation, m as captureFormSnapshot, n as createSearchEngine, o as decomposeTarget, p as diffFormSnapshots, q as find, s as summarizeFormDiff } from './find-l8WZ76Px.js';
export { A as AssertionConfig, a as AssertionExecutor, D as DEFAULT_ASSERTION_CONFIG, c as createAssertionExecutor } from './assertions-Bb5AHWS_.js';
export { b as ANNOTATION_CONFIG_VERSION, A as AnnotationConfig, a as AnnotationCoverage, c as AnnotationEvent, d as AnnotationEventType, E as ElementAnnotation } from './types-C7D5seeQ.js';
export { AnnotationListener, AnnotationStore, getGlobalAnnotationStore, resetGlobalAnnotationStore } from './annotations/index.js';
export { A as AssertionCondition, j as AsyncStrategy, k as SPEC_CONFIG_VERSION, l as SPEC_FILE_EXTENSION, m as SetupAction, e as SpecAssertion, i as SpecAssertionResult, S as SpecCategory, c as SpecConfig, f as SpecCoverage, b as SpecEvent, n as SpecEventType, o as SpecExecutionOptions, g as SpecExecutionResult, d as SpecGroup, h as SpecGroupResult, p as SpecGroupTesting, q as SpecMetadata, a as SpecSeverity, r as SpecSource, s as SpecTarget, t as SpecTestingConfig, T as TestDataAsset, u as TestFixture, v as TestInfrastructureRequirements, w as TestScenario, x as TestStep, V as VALID_ASSERTION_TYPES, y as VALID_SPEC_CATEGORIES, z as VALID_SPEC_SEVERITIES, B as VALID_SPEC_SOURCES } from './types-BXWFvx_x.js';
export { API_CONFIG_VERSION, API_FILE_EXTENSION, ARCHITECTURE_CONFIG_VERSION, ARCHITECTURE_FILE_EXTENSION, AccessibilityTarget, ApiConfig, ApiEndpoint, ArchitectureConfig, ArchitectureConstraint, ArchitecturePattern, ArtifactKind, ArtifactRef, BUILT_IN_CONTEXTS, BrowserTarget, BundleBudget, CONSTRAINT_CONFIG_VERSION, CONSTRAINT_FILE_EXTENSION, CapacityConstraint, ColumnType, ConstraintConfig, DATA_CONFIG_VERSION, DATA_FILE_EXTENSION, DEPENDENCY_CONFIG_VERSION, DEPENDENCY_FILE_EXTENSION, DataColumn, DataConfig, DataEntity, DataIndex, DataMigration, DataModel, DataRelation, DataSeed, DependencyCluster, DependencyConfig, DependencyLink, DirectoryEntry, FeatureDependency, FeatureSpec, HSL, HttpMethod, LegacyTestAssertion, LegacyTestGeneratorOutput, LegacyTestSpecification, LegacyTestTarget, METRIC_FUNCTIONS, ModuleRef, PerformanceBudget, RGBA, ResponsiveBreakpoint, SchemaField, SpecExecutor, SpecExecutorConfig, TechStackEntry, ValidationError, ValidationResult, clusterColors, coerceAssertionType, colorDistance, contrastRatio, createBaseline, diffSnapshots, evaluateConstraint, evaluateQuality, getContext, hueDistance, isGrayscale, isValidAssertionType, isValidSpecCategory, isValidSpecSeverity, isValidSpecSource, listContexts, mergeContext, migrateFromTestGeneratorOutput, migrateLegacyAssertion, migrateLegacyTarget, parseColor, relativeLuminance, resolveTarget, resolveTokenValue, rgbToHsl, runStyleAudit, validateElement, validateSpecAssertion, validateSpecConfig, validateSpecGroup } from './specs/index.js';
export { a as SpecFilterOptions, b as SpecListener, S as SpecStore, g as getGlobalSpecStore, r as resetGlobalSpecStore } from './store-DyHYpiRk.js';
export { D as DesignTokens, c as ElementDiff, E as EvaluateRequest, L as LayoutShift, M as MetricContextConfig, d as MetricFinding, e as MetricFindingSeverity, f as MetricFunction, g as MetricResult, h as QualityContext, Q as QualityEvaluationReport, i as QualityGrade, j as QualityMetricCategory, k as QualityMetricId, l as STYLE_GUIDE_FILE_EXTENSION, m as STYLE_GUIDE_VERSION, n as SnapshotBaseline, b as SnapshotDiffReport, a as StyleAuditReport, o as StyleChange, p as StyleConstraint, q as StyleConstraintResult, S as StyleGuideConfig, r as StyleRule, s as StyleValidationResult, V as ViewportDimensions } from './style-types-DaTmY4r1.js';
export { C as CONFIDENCE_BOOST, a as CONFIDENCE_PENALTY, b as CTR_CONFIG_VERSION, c as CTR_FILE_EXTENSION, d as CtrConfig, e as CtrEntry, f as CtrEntryMetadata, g as CtrEvent, h as CtrEventType, i as CtrListener, j as CtrResolutionResult, k as CtrSelector, l as CtrSelectorStrategy, D as DEFAULT_SELECTOR_CONFIDENCE, E as ElementReliability, M as MIN_CONFIDENCE_THRESHOLD, m as MigrationResult } from './migrate-specs-to-ctr-DNoekGgu.js';
export { AutoPopulateOptions, CentralTargetRegistry, autoPopulateCtr, createCtrEntry, demoteSelector, getGlobalCtr, getViableSelectors, hasViableSelectors, promoteSelector, resetGlobalCtr, setGlobalCtr } from './ctr/index.js';
export { A as AnimationSignalStatus, C as CompositeIdleConfig, a as CompositeIdleStatus, d as CompositeSignalEntry, b as CompositeWaitOptions, D as DOMSettlingConfig, e as DOMSignalStatus, f as DetectedAnimation, g as DetectedLoadingIndicator, F as FormMutationConfig, h as FormMutationSignalStatus, i as IdleEventType, I as IdleSignal, L as LoadingIndicatorConfig, j as LoadingIndicatorSignalStatus, N as NetworkIdleConfig, k as NetworkRequestEndData, l as NetworkRequestStartData, m as NetworkSignalStatus, P as PendingRequest, c as SelectiveWaitOptions, S as SignalStatus, n as SignalTransitionCallback, o as SignalWaitOptions, p as StuckScreenConfig, q as StuckScreenDiagnosis, r as StuckScreenEvidence, s as StuckVerdict, W as WaitTarget } from './types-BFG8zj15.js';
export { DOMSettlingDetector, ElementSettlingOptions, ElementSettlingResult, FormMutationDetector, LoadingIndicatorDetector, NetworkIdleDetector, StuckScreenDetector, waitForElementStable } from './idle/index.js';
export { C as CompositeIdleDetector, a as CompositeTransitionCallback } from './composite-idle-CVbDdYij.js';
export { D as DragDropDetector, M as ModalDetector, N as NavigationTracker, R as RelationshipTracker, S as ShortcutTracker, T as ToastCapture, U as UndoTracker, n as normalizeCombo } from './drag-drop-detector-xq2PQNUI.js';
export { N as NavigationAdapter, a as NavigationRoute, W as WindowLocationAdapter, c as createNextjsAdapter, b as createReactRouterAdapter } from './navigation-adapter-D0eod-Ve.js';
export { d as NetworkEvent, e as NetworkEventCallback, f as NetworkEventType, a as NetworkRequestEntry, g as NetworkRequestFilter, h as NetworkRequestStatus, c as NetworkRequestTracker, N as NetworkTrackerConfig, T as TrackedNetworkRequest, i as TrackedNetworkResponse, W as WaitForRequestOptions, b as WaitForRequestResult } from './tracker-DpZSyunJ.js';
export { a as CONTRACT_CONFIG_VERSION, b as CONTRACT_FILE_EXTENSION, c as ConditionResult, d as ContractCheck, e as ContractCondition, f as ContractConfig, C as ContractExecutionResult, g as ContractMetadata, h as ContractVerification, V as VerificationContract } from './types-DKxy2oHL.js';
export { ContractExecutionOptions, ContractExecutor, ContractValidationError, ContractValidationResult, validateContractConfig } from './contracts/index.js';
export { A as ApiAssertion, a as ApiLayerResult, D as DbAssertion, b as DbLayerResult, E as ExecutionMatrix, c as ExecutionStrategy, F as FsAssertion, d as FsLayerResult, L as LayerAssertionResult, e as LayerSummary, f as LayeredAssertion, g as LayeredAssertionPayload, h as MATRIX_FILE_EXTENSION, M as MatrixExecutionResult, U as UiAssertion, i as UiLayerResult, V as VerificationLayer } from './types-BhENif9B.js';
export { ApiLayerExecutor, IpcLayerExecutor, MatrixExecutor, MatrixExecutorConfig, UiLayerExecutor } from './verification-layers/index.js';
export { A as ArtifactEnvironment, a as ArtifactEvent, b as ArtifactEventType, c as ArtifactListener, d as ArtifactQuery, e as ArtifactResult, f as ArtifactSource, g as ArtifactStore, R as ResultArtifact } from './types-CY_5uaTx.js';
export { IpcArtifactStore, MemoryArtifactStore, captureEnvironment, computeHash, createArtifact, getGlobalArtifactStore, resetGlobalArtifactStore, setGlobalArtifactStore } from './artifacts/index.js';
export { D as DOMChangeEvent } from './types-DhipyJTn.js';
import 'react/jsx-runtime';
import 'react';

/**
 * Network Stub Registry (F2)
 *
 * Server-side registry of fetch-response stubs so tests can short-circuit
 * `window.fetch` calls without hand-rolling a monkey-patch through
 * `page/evaluate`.
 *
 * Design:
 *   - The registry is a module-level singleton (NOT React component state).
 *     Stubs must survive React re-renders, and — per F1 — soft navigations
 *     preserve `window.*` and module scope, so stubs registered before a
 *     `mode: "soft"` navigate keep matching afterwards.
 *   - Stubs clear on hard reload because module state is reinitialised with
 *     the SDK. This is the documented page-lifetime scope.
 *   - Matching is first-registered-wins (FIFO). `times: 1` entries are
 *     consumed on first match; `times: "always"` entries stay until deleted.
 */
type StubMethod = 'GET' | 'POST' | 'PUT' | 'DELETE' | 'PATCH' | '*';
/** Body options: exactly one of `body` / `bodyJson` is required. */
interface StubResponseSpec {
    /** HTTP status code. Default 200. Must be in 100-599. */
    status?: number;
    /** Response headers. Merged with auto `content-type` when `bodyJson` is set. */
    headers?: Record<string, string>;
    /** Raw string body. Mutually exclusive with `bodyJson`. */
    body?: string;
    /** JSON body sugar — stringified & auto-sets `content-type: application/json`. */
    bodyJson?: unknown;
}
interface StubRequestSpec {
    /** Substring match against the request URL (case-sensitive). Required. */
    urlPattern: string;
    /** HTTP method to match. `"*"` matches any. Default `"*"`. */
    method?: StubMethod;
    response: StubResponseSpec;
    /** `"always"` or a positive integer. Default `"always"`. */
    times?: number | 'always';
}
/** A registered stub as returned by `listStubs()`. */
interface StubEntry {
    id: string;
    urlPattern: string;
    method: StubMethod;
    timesRemaining: number | 'always';
    hitCount: number;
}
/** Result of a match lookup. */
interface StubMatch {
    id: string;
    buildResponse: () => Response;
}
interface StubValidationError {
    field: string;
    message: string;
}
/**
 * Validate a StubRequestSpec. Returns the first error (or null if valid).
 * Centralised so the SDK side + Rust handlers can share the same contract.
 */
declare function validateStubRequest(input: unknown): StubValidationError | null;
declare class NetworkStubRegistry {
    private stubs;
    private counter;
    /** Register a new stub. Returns the generated ID. Caller must validate first. */
    register(spec: StubRequestSpec): string;
    /**
     * Look up the first matching stub (FIFO). Consumes a use from `times: N`
     * stubs; removes them when exhausted. Returns null on no match.
     */
    match(url: string, method: string): StubMatch | null;
    /**
     * Non-consuming lookup — N3 `verify-stub`.
     *
     * Same FIFO matching semantics as `match()` but does NOT mutate state:
     *   - `hitCount` is NOT incremented,
     *   - `timesRemaining` is NOT decremented,
     *   - `times: 1` stubs are NOT removed.
     *
     * Returns the same `StubMatch` shape so callers can call `buildResponse()`
     * and report the hypothetical response body/headers/status without
     * actually firing the stub. A snapshot of the matched stub's metadata is
     * exposed via the companion `peekEntry()` helper below — callers that
     * need both the response and the registry-state snapshot typically call
     * `peek()` and `peekEntry()` together.
     *
     * Intended for server-side verification endpoints (e.g.
     * `POST /control/network/verify-stub`) where tests want to ask "would
     * this URL get stubbed?" without consuming a `times: 1` entry.
     */
    peek(url: string, method: string): StubMatch | null;
    /**
     * Companion to `peek()` — returns the matched stub's registry entry
     * (the same shape `list()` returns per stub) without consuming it.
     *
     * Kept separate from `peek()` so the hot `StubMatch` path stays minimal,
     * and so callers that only need the response can skip the extra
     * allocation.
     */
    peekEntry(url: string, method: string): StubEntry | null;
    /** Delete a stub by ID. Returns true if removed, false if unknown. */
    delete(id: string): boolean;
    /** Clear all stubs. Returns the count of stubs removed. */
    clear(): number;
    /** Snapshot of all registered stubs in registration order. */
    list(): StubEntry[];
    /** Test helper: drop all state, reset the ID counter. */
    reset(): void;
    private generateId;
}
/**
 * Access the process-wide stub registry. Module-level so stubs survive
 * React re-renders and F1 soft navigations.
 */
declare function getGlobalStubRegistry(): NetworkStubRegistry;
/** Test helper: replace the singleton with a fresh registry. */
declare function __resetGlobalStubRegistryForTest(): void;
/**
 * Wrap `window.fetch` with a stub-matcher that short-circuits matching URLs.
 * Safe to call multiple times; only the first call replaces fetch. Unmatched
 * requests fall through to the real fetch. Intended for non-browser-SDK hosts
 * (e.g. the Tauri runner) that manage stub lifecycle via their own command
 * dispatcher and need the stub short-circuit without the full
 * `installNetworkInterceptor` (which also owns network-request tracking).
 */
declare function installStubFetchInterceptor(): void;

/**
 * Undo/Redo Detector
 *
 * Stateless on-demand DOM scanner for undo/redo availability.
 * Detects undo/redo buttons, menu items, and toolbar elements using:
 * - ARIA attributes (aria-label, aria-keyshortcuts)
 * - Title attributes
 * - Text content matching
 * - Common UI framework patterns
 * - document.execCommand probe (for contentEditable contexts)
 *
 * Follows the same stateless pattern as ModalDetector.
 */

interface UndoDetectionResult {
    /** Detected undo element info */
    undoElement?: UndoElementInfo;
    /** Detected redo element info */
    redoElement?: UndoElementInfo;
    /** Whether undo is available via execCommand */
    execCommandUndo: boolean;
    /** Whether redo is available via execCommand */
    execCommandRedo: boolean;
    /** Timestamp */
    timestamp: number;
}
declare class UndoDetector {
    private readonly config;
    constructor(config?: UndoDetectorConfig);
    /**
     * Scan the DOM for undo/redo availability.
     */
    detect(): UndoDetectionResult;
}

/**
 * ToastRingBuffer — persistent log of emitted toasts for after-the-fact queries.
 *
 * Unlike `ToastCapture` (which scans the DOM for transient notification elements),
 * this is an *explicit* log that consumers emit into when they show status
 * messages. Survives the toast's visual dismissal so tests can assert on the
 * banner text well after it has unmounted.
 *
 * Size-capped ring buffer (default 200 entries) with a TTL (default 10 minutes).
 * When either cap is hit, the oldest entries are evicted.
 *
 * Exported as a singleton (`toastBuffer`) since every consumer within a given
 * app shares one buffer — the runner fetches it through the IPC bridge.
 */
type ToastKind = 'info' | 'success' | 'warning' | 'error';
interface ToastEntry {
    /** Stable identifier. Auto-generated if not provided on emit. */
    id: string;
    /** Banner text. */
    message: string;
    /** Severity. */
    kind: ToastKind;
    /** Epoch ms when the toast was emitted. */
    timestamp: number;
    /** Epoch ms when `markDismissed(id)` was called (if ever). */
    dismissedAt?: number;
}
interface EmitToastInput {
    id?: string;
    message: string;
    kind: ToastKind;
    /** Epoch ms; defaults to `Date.now()`. Useful in tests. */
    timestamp?: number;
}
interface ToastRingBufferOptions {
    /** Max entries retained. Defaults to 200. */
    maxEntries?: number;
    /** Entry TTL in ms. Defaults to 600_000 (10 min). */
    ttlMs?: number;
    /** Optional clock override for tests. */
    now?: () => number;
}
/** Subscriber callback. Receives the full current buffer snapshot. */
type ToastSubscriber = (entries: ReadonlyArray<ToastEntry>) => void;
declare class ToastRingBuffer {
    private readonly maxEntries;
    private readonly ttlMs;
    private readonly now;
    private entries;
    private subscribers;
    private idCounter;
    constructor(options?: ToastRingBufferOptions);
    /**
     * Append a toast to the buffer. Returns the finalized entry (with
     * generated id + timestamp filled in if omitted).
     */
    emit(input: EmitToastInput): ToastEntry;
    /**
     * Mark a previously-emitted toast as dismissed. No-op if the id is
     * unknown. Safe to call multiple times (only the first call sets the
     * timestamp).
     */
    markDismissed(id: string): void;
    /**
     * Return every entry with `timestamp >= sinceMs`, optionally truncated
     * to `limit` most-recent entries. Output is sorted most-recent-first.
     *
     * `sinceMs` is inclusive so callers can use the timestamp from a prior
     * call as the new `sinceMs` without missing entries.
     */
    getSince(sinceMs: number | undefined, limit?: number): ToastEntry[];
    /** Full current buffer, oldest-first. */
    getAll(): ToastEntry[];
    /** Current buffer size (post-eviction). */
    size(): number;
    /** Drop all entries. Notifies subscribers. */
    clear(): void;
    /** Subscribe to changes; returns an unsubscribe fn. */
    subscribe(callback: ToastSubscriber): () => void;
    private evict;
    private notify;
}
/**
 * Module-wide singleton. The runner side fetches from this instance via the
 * IPC bridge, so every emit within the running app lands in the same buffer.
 */
declare const toastBuffer: ToastRingBuffer;
/**
 * Convenience helper: emit a toast into the global ring buffer.
 *
 * Usage in components that render their own inline status banner (like
 * `DiscoveryPanel.tsx`'s `setStatus`):
 *
 * ```ts
 * import { emitToast } from '@qontinui/ui-bridge';
 * emitToast({ message: `Registered "${app.name}"`, kind: 'success' });
 * ```
 */
declare function emitToast(input: EmitToastInput): ToastEntry;

/**
 * useUIBridgeToasts — React hook that mirrors the toast ring buffer into
 * React state so consumers can render lists of captured toasts, or wire the
 * buffer into their own toast library's lifecycle callbacks.
 *
 * Two integration patterns:
 *
 * 1. **Library lifecycle wiring** (sonner, react-hot-toast) — pass
 *    `{ message, kind }` into the library's `<Toaster onCreate>` /
 *    `onDismiss` hooks and also call `emitToast(...)` + `markDismissed(id)`.
 *    Typically handled once per app:
 *
 *    ```tsx
 *    <Toaster
 *      onCreate={(t) => emitToast({ id: String(t.id), message: t.message, kind: t.type })}
 *      onDismiss={(t) => toastBuffer.markDismissed(String(t.id))}
 *    />
 *    ```
 *
 * 2. **Inline status banners** — the component renders its own banner (e.g.
 *    `setStatus({kind:'success', message:'Registered'})`); call `emitToast`
 *    in the same effect that sets the status:
 *
 *    ```tsx
 *    setStatus({ kind, message });
 *    emitToast({ kind, message });
 *    ```
 */

/**
 * Read the live toast buffer as a React value. Re-renders on every
 * emit / markDismissed.
 */
declare function useUIBridgeToasts(): ReadonlyArray<ToastEntry>;

/**
 * Framework Adapter Types
 *
 * Defines the interface for opt-in form framework adapters that extract
 * form state directly from library internals (React Hook Form, Formik, etc.),
 * bypassing DOM heuristics for perfect accuracy.
 *
 * Adapters are opt-in: users register them explicitly. Each adapter is a
 * separate import so unused adapters are tree-shaken.
 */

/** Error information extracted from a framework adapter */
interface AdapterFieldError {
    /** Human-readable error message */
    message: string;
    /** Error type/rule name (e.g., 'required', 'minLength', 'custom') */
    type: string;
}
/** Adapter for extracting form state from a specific form library */
interface FormFrameworkAdapter {
    /** Unique name of the adapter (e.g., 'react-hook-form', 'formik') */
    readonly name: string;
    /** Check if this adapter can handle the current page/form */
    detect(): boolean;
    /** Extract form states from the framework's internal state */
    extractForms(): FormState[];
    /** Get validation errors for a specific field */
    getFieldErrors(fieldId: string): AdapterFieldError[] | null;
    /** Check if a specific field has been touched/interacted with */
    isFieldTouched(fieldId: string): boolean | null;
    /** Check if a specific field's value differs from its initial value */
    isFieldDirty(fieldId: string): boolean | null;
}
/** Registry for framework adapters */
interface AdapterRegistry {
    /** Register a framework adapter */
    register(adapter: FormFrameworkAdapter): void;
    /** Unregister a framework adapter by name */
    unregister(name: string): void;
    /** Get a specific adapter by name */
    getAdapter(name: string): FormFrameworkAdapter | undefined;
    /** Run detection on all registered adapters, return those that match */
    detectAdapters(): FormFrameworkAdapter[];
    /** Get all registered adapters (whether detected or not) */
    getAllAdapters(): FormFrameworkAdapter[];
}

/**
 * Adapter Registry
 *
 * Simple map-based registry for form framework adapters.
 * Consumers register adapters explicitly; detectAdapters() probes
 * each registered adapter and returns those whose detect() returns true.
 */

/**
 * Create a new adapter registry.
 *
 * @example
 * ```ts
 * import { createAdapterRegistry, ReactHookFormAdapter } from '@qontinui/ui-bridge';
 *
 * const registry = createAdapterRegistry();
 * registry.register(new ReactHookFormAdapter());
 *
 * // Later, detect which adapters are active on the page
 * const active = registry.detectAdapters();
 * for (const adapter of active) {
 *   const forms = adapter.extractForms();
 *   console.log(`${adapter.name}: ${forms.length} forms`);
 * }
 * ```
 */
declare function createAdapterRegistry(): AdapterRegistry;

/**
 * React Hook Form Adapter
 *
 * Extracts form state from React Hook Form's internal state, bypassing
 * DOM heuristics for perfect accuracy. Uses multiple strategies:
 *
 * 1. `window.__RHF_DEVTOOLS__` — when RHF DevTools extension is installed
 * 2. React fiber tree walking — finds useForm state in component fibers
 * 3. DOM fallback — RHF-specific selectors and data attributes
 *
 * For guaranteed accuracy, apps can expose their form instances via
 * `window.__UI_BRIDGE_RHF_FORMS__` as a Map<string, UseFormReturn>.
 * The adapter will prefer this over fiber walking.
 *
 * @example
 * ```ts
 * import { createAdapterRegistry, ReactHookFormAdapter } from '@qontinui/ui-bridge';
 *
 * const registry = createAdapterRegistry();
 * registry.register(new ReactHookFormAdapter());
 * ```
 *
 * For guaranteed accuracy in your app:
 * ```ts
 * // In your app code — expose form instances on a well-known global
 * const form = useForm({ defaultValues: { email: '', password: '' } });
 *
 * useEffect(() => {
 *   window.__UI_BRIDGE_RHF_FORMS__ = window.__UI_BRIDGE_RHF_FORMS__ || new Map();
 *   window.__UI_BRIDGE_RHF_FORMS__.set('login-form', form);
 *   return () => { window.__UI_BRIDGE_RHF_FORMS__?.delete('login-form'); };
 * }, [form]);
 * ```
 *
 * If no explicit instances are provided, the adapter falls back to
 * RHF DevTools (`window.__RHF_DEVTOOLS__`) and React fiber walking
 * to auto-detect forms on the page.
 */

/** Minimal shape of a React Hook Form `UseFormReturn` we depend on */
interface RHFFormInstance {
    formState: {
        errors: Record<string, RHFFieldError | undefined>;
        touchedFields: Record<string, boolean | undefined>;
        dirtyFields: Record<string, boolean | undefined>;
        isValid: boolean;
        isDirty: boolean;
    };
    getValues: () => Record<string, unknown>;
}
interface RHFFieldError {
    message?: string;
    type?: string;
    types?: Record<string, string | boolean>;
}
/** Shape exposed by RHF DevTools */
interface RHFDevTools {
    forms?: Map<string, RHFFormInstance> | Record<string, RHFFormInstance>;
}
/** Augment the global Window interface */
declare global {
    interface Window {
        /** RHF DevTools extension */
        __RHF_DEVTOOLS__?: RHFDevTools;
        /** Explicit form instances provided by the app for guaranteed accuracy */
        __UI_BRIDGE_RHF_FORMS__?: Map<string, RHFFormInstance>;
        /** React DevTools hook (used for fiber walking) */
        __REACT_DEVTOOLS_GLOBAL_HOOK__?: {
            renderers?: Map<number, {
                findFiberByHostInstance?: (instance: Element) => unknown;
            }>;
        };
    }
}
declare class ReactHookFormAdapter implements FormFrameworkAdapter {
    readonly name = "react-hook-form";
    private cachedInstances;
    detect(): boolean;
    extractForms(): FormState[];
    getFieldErrors(fieldId: string): AdapterFieldError[] | null;
    isFieldTouched(fieldId: string): boolean | null;
    isFieldDirty(fieldId: string): boolean | null;
    /** Collect all RHF form instances from available sources */
    private getInstances;
    /** Walk React fiber tree to find RHF's useForm state */
    private findRHFInstanceInFiber;
    /** Check if a fiber node contains RHF-shaped state */
    private extractRHFFromFiber;
    /** Shape-check: does this value look like a UseFormReturn? */
    private isRHFInstance;
    /** Convert an RHF form instance to the UI Bridge FormState shape */
    private instanceToFormState;
    /** Normalize an RHF error into AdapterFieldError[] */
    private normalizeError;
    /** Best-effort field label extraction */
    private getFieldLabel;
    /** Stringify a form value for the FormFieldState shape */
    private stringifyValue;
}

/**
 * Formik Adapter
 *
 * Extracts form state from Formik's internal state, bypassing DOM heuristics
 * for perfect accuracy. Uses multiple strategies:
 *
 * 1. `window.__UI_BRIDGE_FORMIK_FORMS__` — explicit app-provided instances
 * 2. React fiber tree walking — finds Formik context providers in component fibers
 * 3. DOM fallback — Formik-specific data attributes
 *
 * For guaranteed accuracy, apps can expose their Formik instances via
 * `window.__UI_BRIDGE_FORMIK_FORMS__` as a Map<string, FormikContextType>.
 *
 * @example
 * ```ts
 * import { createAdapterRegistry, FormikAdapter } from '@qontinui/ui-bridge';
 *
 * const registry = createAdapterRegistry();
 * registry.register(new FormikAdapter());
 * ```
 *
 * For guaranteed accuracy in your app:
 * ```ts
 * // In your app code, inside a Formik context
 * const formik = useFormikContext();
 *
 * useEffect(() => {
 *   window.__UI_BRIDGE_FORMIK_FORMS__ = window.__UI_BRIDGE_FORMIK_FORMS__ || new Map();
 *   window.__UI_BRIDGE_FORMIK_FORMS__.set('signup-form', formik);
 *   return () => { window.__UI_BRIDGE_FORMIK_FORMS__?.delete('signup-form'); };
 * }, [formik]);
 * ```
 *
 * If no explicit instances are provided, the adapter falls back to
 * Formik-specific DOM attributes (`data-formik-id`, `data-formik`) and
 * React fiber walking to auto-detect forms on the page.
 */

/** Minimal shape of Formik's context/state we depend on */
interface FormikInstance {
    values: Record<string, unknown>;
    errors: Record<string, string | undefined>;
    touched: Record<string, boolean | undefined>;
    dirty: boolean;
    isValid: boolean;
    initialValues: Record<string, unknown>;
}
/** Augment the global Window interface */
declare global {
    interface Window {
        /** Explicit Formik instances provided by the app for guaranteed accuracy */
        __UI_BRIDGE_FORMIK_FORMS__?: Map<string, FormikInstance>;
    }
}
declare class FormikAdapter implements FormFrameworkAdapter {
    readonly name = "formik";
    private cachedInstances;
    detect(): boolean;
    extractForms(): FormState[];
    getFieldErrors(fieldId: string): AdapterFieldError[] | null;
    isFieldTouched(fieldId: string): boolean | null;
    isFieldDirty(fieldId: string): boolean | null;
    /** Collect all Formik form instances from available sources */
    private getInstances;
    /** Walk React fiber tree to find Formik context/state */
    private findFormikInstanceInFiber;
    /** Check if a fiber node contains Formik-shaped state */
    private extractFormikFromFiber;
    /** Shape-check: does this value look like a Formik context? */
    private isFormikInstance;
    /** Convert a Formik instance to the UI Bridge FormState shape */
    private instanceToFormState;
    /** Best-effort field label extraction */
    private getFieldLabel;
    /** Stringify a form value for the FormFieldState shape */
    private stringifyValue;
}

export { type AdapterFieldError, type AdapterRegistry, type EmitToastInput, type FormFrameworkAdapter, FormState, FormikAdapter, NetworkStubRegistry, ReactHookFormAdapter, type StubEntry, type StubMatch, type StubMethod, type StubRequestSpec, type StubResponseSpec, type StubValidationError, type ToastEntry, type ToastKind, ToastRingBuffer, type ToastRingBufferOptions, type ToastSubscriber, type UndoDetectionResult, UndoDetector, UndoDetectorConfig, UndoElementInfo, __resetGlobalStubRegistryForTest, createAdapterRegistry, emitToast, getGlobalStubRegistry, installStubFetchInterceptor, toastBuffer, useUIBridgeToasts, validateStubRequest };
