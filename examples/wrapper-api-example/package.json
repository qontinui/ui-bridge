{
  "name": "ui-bridge-wrapper-api-example",
  "version": "0.1.0",
  "private": true,
  "description": "Reference api-transport wrapper. Wraps a remote HTTP API behind the UI Bridge action vocabulary using @qontinui/ui-bridge-wrapper's ApiTransport — caller-driven, in-process, no browser, no WebSocket.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node --import tsx src/index.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@qontinui/ui-bridge-wrapper": "*"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0",
    "vitest": "^4.1.0"
  }
}
