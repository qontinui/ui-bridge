{
  "name": "ui-bridge-wrapper-live-example",
  "version": "0.1.0",
  "private": true,
  "description": "Reference WebSocket-transport wrapper. Connects to a qontinui runner's /ui-bridge/ws endpoint and serves a representative subset of the relay action vocabulary so adopters can see the WS dispatch path end-to-end.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node --import tsx src/index.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@qontinui/ui-bridge-wrapper": "*",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/ws": "^8.5.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0",
    "vitest": "^4.1.0"
  }
}
